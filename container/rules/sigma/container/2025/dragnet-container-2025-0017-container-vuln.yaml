title: 'Dragnet [Container]: dragnet-container-2025-0017 — Vulnerable Base Image Pull'
id: b66e9559-1a65-59ae-9d65-3715732d5152
status: deprecated
description: |
  Detects deployment of a container image known to contain a critical vulnerability.
  Incident: dragnet-container-2025-0017 (Tier 2, CVSS 9.8)
  MapServer is a system for developing web-based GIS applications. Prior to 8.4.1, the XML Filter Query directive PropertyName is vulnerably to Boolean-based SQL injection. It seems like expression checking is bypassed by introducing double quote characters in the PropertyName. Allowing to manipulate backend database queries. This vulnerability is fixed in 8.4.1.
  First observed: 2025-10-08.
references:
  - https://github.com/MapServer/MapServer/security/advisories/GHSA-256m-rx4h-r55w
author: dragnet-bot
date: 2026-05-22
tags:
  - attack.t1610
  - attack.t1611
  - dragnet.cve
logsource:
  category: container_image_pull
detection:
  selection:
    Image|contains:
      - 'debian:11'
      - 'debian:12'
      - 'debian:13'
      - 'debian:14'
  condition: selection
falsepositives:
  - Cached images pulled before the vulnerability was published
  - Internal image mirrors that have not been updated
level: critical
