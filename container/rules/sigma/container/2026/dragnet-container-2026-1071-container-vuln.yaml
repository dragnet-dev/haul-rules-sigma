title: 'Dragnet [Container]: dragnet-container-2026-1071 — Vulnerable Base Image Pull'
id: eefaf1a0-bedf-590f-8c8e-0563f722d26f
status: deprecated
description: |
  Detects deployment of a container image known to contain a critical vulnerability.
  Incident: dragnet-container-2026-1071 (Tier 2, CVSS 9.8)
  llama.cpp is an inference of several LLM models in C/C++. In commits 55d4206c8 and prior, the n_discard parameter is parsed directly from JSON input in the llama.cpp server's completion endpoints without validation to ensure it's non-negative. When a negative value is supplied and the context fills up, llama_memory_seq_rm/add receives a reversed range and negative offset, causing out-of-bounds memory writes in the token evaluation loop. This deterministic memory corruption can crash the process or enable remote code execution (RCE). There is no fix at the time of publication.
  First observed: 2026-02-02.
references:
  - https://access.redhat.com/security/cve/CVE-2026-21869
  - https://github.com/ggml-org/llama.cpp/security/advisories/GHSA-8947-pfff-2f3c
  - https://nvd.nist.gov/vuln/detail/CVE-2026-21869
  - https://www.cve.org/CVERecord?id=CVE-2026-21869
author: dragnet-bot
date: 2026-05-20
tags:
  - attack.t1610
  - attack.t1611
  - dragnet.cve
logsource:
  category: container_image_pull
detection:
  selection:
    Image|contains:
      - 'debian:14'
  condition: selection
falsepositives:
  - Cached images pulled before the vulnerability was published
  - Internal image mirrors that have not been updated
level: critical
