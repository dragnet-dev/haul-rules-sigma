title: 'Dragnet [Container]: dragnet-container-2026-2054 — Vulnerable Base Image Pull'
id: c833fd1a-9c4f-5fb2-a1b3-6ac170e4f1ab
status: deprecated
description: |
  Detects deployment of a container image known to contain a critical vulnerability.
  Incident: dragnet-container-2026-2054 (Tier 2, CVSS 9.8)
  rust-openssl provides OpenSSL bindings for the Rust programming language. From to before 0.10.78, aes::unwrap_key() contains an incorrect assertion: it checks that out.len() + 8 <= in_.len(), but this condition is reversed. The intended invariant is out.len() >= in_.len() - 8, ensuring the output buffer is large enough. Because of the inverted check, the function only accepts buffers at or below the minimum required size and rejects larger ones. If a smaller buffer is provided the function will write past the end of out by in_.len() - 8 - out.len() bytes, causing an out-of-bounds write from a safe public function. This vulnerability is fixed in 0.10.78.
  First observed: 2026-04-28.
references:
  - https://github.com/rust-openssl/rust-openssl
  - https://github.com/rust-openssl/rust-openssl/commit/718d07ff8ff7be417d5b7a6a0047f1607520b3b6
  - https://github.com/rust-openssl/rust-openssl/pull/2604
  - https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78
  - https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-8c75-8mhr-p7r9
  - https://nvd.nist.gov/vuln/detail/CVE-2026-41678
  - https://www.cve.org/CVERecord?id=CVE-2026-41678
author: dragnet-bot
date: 2026-05-28
tags:
  - attack.t1610
  - attack.t1611
  - dragnet.cve
logsource:
  category: container_image_pull
detection:
  selection:
    Image|contains:
      - 'chainguard:'
      - 'debian:11'
      - 'debian:12'
      - 'debian:13'
      - 'debian:14'
      - 'ubuntu:22.04'
      - 'ubuntu:24.04'
      - 'ubuntu:25.10'
      - 'ubuntu:26.04'
      - 'wolfi:'
  condition: selection
falsepositives:
  - Cached images pulled before the vulnerability was published
  - Internal image mirrors that have not been updated
level: critical
